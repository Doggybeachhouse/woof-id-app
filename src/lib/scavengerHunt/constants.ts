/** Active hunt route — Losloopgebied Zuid (coordinates supplied by DBH). */
export const ZUID_HUNT_SLUG = "zuid";

/** @deprecated Old full-Zandvoort route; kept for existing progress rows. */
export const ZANDVOORT_HUNT_SLUG = "zandvoort-dunes";

export const ACTIVE_HUNT_SLUG = ZUID_HUNT_SLUG;

/** Reject GPS readings worse than this (meters). */
export const MAX_GPS_ACCURACY_METERS = 100;

export const HUNT_COMPLETION_COINS = 50;

/** Fixed price per paid hunt hint (EUR). */
export const HUNT_HINT_PRICE_EUR = "1.50";
